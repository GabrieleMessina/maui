<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:views="clr-namespace:Samples.View"
                xmlns:viewmodels="clr-namespace:Samples.ViewModel"
                x:Class="Samples.View.FolderPickerPage"
                Title="Folder picker">
    <views:BasePage.BindingContext>
        <viewmodels:FolderPickerViewModel />
    </views:BasePage.BindingContext>
    <StackLayout Padding="12" Spacing="6">
        <Label Text="Pick folder from storage." FontAttributes="Bold" />

        <Button Text="Pick folder" Command="{Binding PickFolderCommand}" HorizontalOptions="FillAndExpand" />

        <Label Text="{Binding Text}" HorizontalOptions="FillAndExpand" />
    </StackLayout>
</views:BasePage>